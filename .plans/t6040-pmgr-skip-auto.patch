diff --git a/drivers/pmdomain/apple/pmgr-pwrstate.c b/drivers/pmdomain/apple/pmgr-pwrstate.c
--- a/drivers/pmdomain/apple/pmgr-pwrstate.c
+++ b/drivers/pmdomain/apple/pmgr-pwrstate.c
@@ -282,14 +282,17 @@ static int apple_pmgr_ps_probe(struct platform_device *pdev)
 	}
 
 	/* Turn on auto-PM if the domain is already on */
-	if (active) {
+	if (active && of_property_read_bool(node, "apple,skip-auto-enable")) {
+		dev_info(dev, "PMGRDBG %s offset %#x: auto-enable write skipped by DT\n",
+			 name, ps->offset);
+	} else if (active) {
 		dev_info(dev, "PMGRDBG %s offset %#x: auto-enable write begin\n",
 			 name, ps->offset);
 		regmap_update_bits(regmap, ps->offset, APPLE_PMGR_FLAGS | APPLE_PMGR_AUTO_ENABLE,
 				   APPLE_PMGR_AUTO_ENABLE);
 		dev_info(dev, "PMGRDBG %s offset %#x: auto-enable write done\n",
 			 name, ps->offset);
 	}
 
 	dev_info(dev, "PMGRDBG %s offset %#x: genpd registration begin\n",
 		 name, ps->offset);
